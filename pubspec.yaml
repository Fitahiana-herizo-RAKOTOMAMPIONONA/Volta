name: volta
description: Application de test de puissance pour chargeurs et câbles

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.8
  syncfusion_flutter_gauges: ^24.2.9
  
  # State Management
  flutter_hooks: ^0.21.2
  flutter_bloc: ^9.1.1
  
  # Permissions
  permission_handler: ^12.0.1
  
  # Data Storage
  shared_preferences: ^2.2.2
  
  # Charts (alternative to Syncfusion)
  fl_chart: ^0.68.0
  
  # Utils
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
  
  # # Fonts (optionnel)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700